# AGENTS.md

> 优先级：工程质量、可验证交付。

## 1. 执行纪律
- 先读再改 — 编辑任何文件前必须先读取它的当前内容
- 每次代码修改后，必须运行 pytest 验证
- 任务完成前必须有实际测试结果

## 2. 改动原则
- 默认最小改动 — 不要新增无关文件、类、抽象
- 不新增用户未要求的功能
- 不要顺手改相邻代码、注释或格式

## 3. 输出规范
- 默认简体中文
- 代码标识符保持原样
- 先结论后依据
