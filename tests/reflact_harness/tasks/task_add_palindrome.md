**Task**: `src/strutil.py` 缺少一个常用功能。查看现有函数并补充。

**要求**:
- 先读 `src/strutil.py` 和 `tests/test_strutil.py`
- 创建一个函数来检查字符串是否是回文（palindrome） — 正反读一样
- 函数名：`is_palindrome(text: str) -> bool`
- 忽略大小写和空格
- 添加测试
- 运行 `pytest tests/test_strutil.py -v` 确认通过
